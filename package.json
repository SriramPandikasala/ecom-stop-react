{
  "name": "ecom-stop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:install": "npm install",
    "dev:start:server": "npm run dev:start --workspace=ui-server",
    "dev:start:ui": "npm run build --workspace=ui",
    "start:dev": "npm-run-all --parallel dev:start:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "ui",
    "ui-server"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
